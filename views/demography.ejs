<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <nav>
        <h1><%= title %></h1>
        <a class="delete" data-demo-id=``>Delete</a>
    </nav>
    
        <% if(models.length>0){ %>
            <section>
                <% models.forEach(model=>{ %>
                    <%- include('partials/model', {model}) %>
                <% }) %>
            </section>
        <% } %>

    
    <%- include('partials/confirmDeletion',{url:`/demographics/${demographic.id}`}) %>
</body>
</html>