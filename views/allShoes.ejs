<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <%- include('partials/header') %>
    <nav>
        <h1><%= title %></h1>
    </nav>
        <% if(shoes.length>0){ %>
            <section>
                <% shoes.forEach(shoe=>{ %>
                    <%- include('partials/shoe',{shoe}) %>  
                <% }) %>
            </section>
        <% } %>
</body>
</html>